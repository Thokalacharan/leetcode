{
  "name": "codepulse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  }
}
